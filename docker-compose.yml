services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3100:80"
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3010:80"